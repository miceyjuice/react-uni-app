import styled from "styled-components";
import { InfoBox } from "../Proposals/ProposalsStyle";

export const CustomBox = styled(InfoBox)`
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
`;
